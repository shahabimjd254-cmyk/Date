<!DOCTYPE html> <html lang="fa"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>محاسبه سن حرفه‌ای</title> <style>     body {         font-family: Tahoma, sans-serif;         background: linear-gradient(to right, #a1c4fd, #c2e9fb);         display: flex;         flex-direction: column;         align-items: center;         justify-content: center;         height: 100vh;         margin: 0;         color: #333;     }     h1 {         margin-bottom: 20px;         color: #1a73e8;     }     .container {         background-color: #fff;         padding: 30px;         border-radius: 20px;         box-shadow: 0 10px 30px rgba(0,0,0,0.1);         text-align: center;         max-width: 400px;         width: 90%;     }     input, select, button {         padding: 10px;         margin: 10px 0;         font-size: 16px;         width: 100%;         border-radius: 10px;         border: 1px solid #ccc;     }     .result {         margin-top: 20px;         font-size: 18px;         font-weight: bold;         color: #1a73e8;     } </style> </head> <body>  <div class="container">     <h1>محاسبه سن حرفه‌ای</h1>          <label>انتخاب نوع تقویم:</label>     <select id="calendarType">         <option value="gregorian">میلادی</option>         <option value="jalali">شمسی</option>     </select>      <label>تاریخ تولد خود را وارد کنید:</label>     <input type="date" id="birthdate">     <button onclick="startAgeCalculation()">محاسبه سن</button>      <div class="result" id="result"></div> </div>  <script src="https://cdnjs.cloudflare.com/ajax/libs/jalaali-js/1.1.0/jalaali.min.js"></script> <script> let intervalId;  function startAgeCalculation() {     clearInterval(intervalId); // توقف به‌روزرسانی قبلی     const birthInput = document.getElementById('birthdate').value;     if (!birthInput) {         document.getElementById('result').innerText = "لطفا تاریخ تولد را وارد کنید!";         return;     }          const calendarType = document.getElementById('calendarType').value;     const birthDate = new Date(birthInput);      intervalId = setInterval(() => {         const now = new Date();         let ageYears, ageMonths, ageDays, ageHours, ageMinutes, ageSeconds;          if (calendarType === "gregorian") {             ageYears = now.getFullYear() - birthDate.getFullYear();             ageMonths = now.getMonth() - birthDate.getMonth();             ageDays = now.getDate() - birthDate.getDate();         } else {             const nowJ = jalaali.toJalaali(now);             const birthJ = jalaali.toJalaali(birthDate);             ageYears = nowJ.jy - birthJ.jy;             ageMonths = nowJ.jm - birthJ.jm;             ageDays = nowJ.jd - birthJ.jd;         }          if (ageDays < 0) {             ageMonths--;             ageDays += 30; // تقریباً برای شمسی و میلادی         }         if (ageMonths < 0) {             ageYears--;             ageMonths += 12;         }          ageHours = now.getHours() - birthDate.getHours();         ageMinutes = now.getMinutes() - birthDate.getMinutes();         ageSeconds = now.getSeconds() - birthDate.getSeconds();          if (ageSeconds < 0) { ageMinutes--; ageSeconds += 60; }         if (ageMinutes < 0) { ageHours--; ageMinutes += 60; }         if (ageHours < 0) { ageDays--; ageHours += 24; }          document.getElementById('result').innerText =              `سن شما: ${ageYears} سال، ${ageMonths} ماه، ${ageDays} روز، ` +             `${ageHours} ساعت، ${ageMinutes} دقیقه و ${ageSeconds} ثانیه`;     }, 1000); } </script>  </body> </html><!DOCTYPE html> <html lang="fa"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>محاسبه سن حرفه‌ای</title> <style>     body {         font-family: Tahoma, sans-serif;         background: linear-gradient(to right, #a1c4fd, #c2e9fb);         display: flex;         flex-direction: column;         align-items: center;         justify-content: center;         height: 100vh;         margin: 0;         color: #333;     }     h1 {         margin-bottom: 20px;         color: #1a73e8;     }     .container {         background-color: #fff;         padding: 30px;         border-radius: 20px;         box-shadow: 0 10px 30px rgba(0,0,0,0.1);         text-align: center;         max-width: 400px;         width: 90%;     }     input, select, button {         padding: 10px;         margin: 10px 0;         font-size: 16px;         width: 100%;         border-radius: 10px;         border: 1px solid #ccc;     }     .result {         margin-top: 20px;         font-size: 18px;         font-weight: bold;         color: #1a73e8;     } </style> </head> <body>  <div class="container">     <h1>محاسبه سن حرفه‌ای</h1>          <label>انتخاب نوع تقویم:</label>     <select id="calendarType">         <option value="gregorian">میلادی</option>         <option value="jalali">شمسی</option>     </select>      <label>تاریخ تولد خود را وارد کنید:</label>     <input type="date" id="birthdate">     <button onclick="startAgeCalculation()">محاسبه سن</button>      <div class="result" id="result"></div> </div>  <script src="https://cdnjs.cloudflare.com/ajax/libs/jalaali-js/1.1.0/jalaali.min.js"></script> <script> let intervalId;  function startAgeCalculation() {     clearInterval(intervalId); // توقف به‌روزرسانی قبلی     const birthInput = document.getElementById('birthdate').value;     if (!birthInput) {         document.getElementById('result').innerText = "لطفا تاریخ تولد را وارد کنید!";         return;     }          const calendarType = document.getElementById('calendarType').value;     const birthDate = new Date(birthInput);      intervalId = setInterval(() => {         const now = new Date();         let ageYears, ageMonths, ageDays, ageHours, ageMinutes, ageSeconds;          if (calendarType === "gregorian") {             ageYears = now.getFullYear() - birthDate.getFullYear();             ageMonths = now.getMonth() - birthDate.getMonth();             ageDays = now.getDate() - birthDate.getDate();         } else {             const nowJ = jalaali.toJalaali(now);             const birthJ = jalaali.toJalaali(birthDate);             ageYears = nowJ.jy - birthJ.jy;             ageMonths = nowJ.jm - birthJ.jm;             ageDays = nowJ.jd - birthJ.jd;         }          if (ageDays < 0) {             ageMonths--;             ageDays += 30; // تقریباً برای شمسی و میلادی         }         if (ageMonths < 0) {             ageYears--;             ageMonths += 12;         }          ageHours = now.getHours() - birthDate.getHours();         ageMinutes = now.getMinutes() - birthDate.getMinutes();         ageSeconds = now.getSeconds() - birthDate.getSeconds();          if (ageSeconds < 0) { ageMinutes--; ageSeconds += 60; }         if (ageMinutes < 0) { ageHours--; ageMinutes += 60; }         if (ageHours < 0) { ageDays--; ageHours += 24; }          document.getElementById('result').innerText =              `سن شما: ${ageYears} سال، ${ageMonths} ماه، ${ageDays} روز، ` +             `${ageHours} ساعت، ${ageMinutes} دقیقه و ${ageSeconds} ثانیه`;     }, 1000); } </script>  </body> </html>
